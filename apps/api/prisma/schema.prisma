generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Models will be added as modules are implemented.
// See docs/specs/docs-prd-modules-spec/ for entity specs.
